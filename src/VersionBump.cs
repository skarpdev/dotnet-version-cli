namespace Skarp.Version.Cli
{
    /// <summary>
    /// Enumerates the possible version bumps
    /// </summary>
    public enum VersionBump
    {
        Major,

        Minor,

        Patch
    }
}