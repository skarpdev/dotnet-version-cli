namespace Skarp.Version.Cli.CsProj
{
    public enum ProjectFileProperty
    {
        Version,
        PackageVersion,
        PackageId,
        Title,
        VersionPrefix,
        VersionSuffix
    }
}
